```Java
/*Conversão de dados Strings em dados Double
Ex:  */

String precoSTR = "9,99"; /*STR para marcar que é uma string. Caso queria fazer alguma
//Tentar fazer a conta// 	conta não será possivel*/	
precoSTR = precoSTR +"1";
System.out.println(precoSTR);

R:"9.991" ;
/*Isso acontece porque o operador + opera em strings, ele acaba juntando as strings ou
seja, concatenando as strings*/

String precoSTR = "9,99"; /* para operar de forma certa, deve-se converter a string em 
algum outro tipo primitivo*/

double preco = Double.parseDouble(precoSTR); /*Nesse caso o String precoSTR deve ir convertido para a variavel que 
sera feita a conversão*/

System.out.println(precoSTR); /* Será impresso em dados tipo Strings*/
System.out.println(preco0; /* Será impresso em dados tipo Double*/

/*Em numero Double é usado o:*/ double aDouble = Double.parseDouble(aString); 
 /* Em Numero inteiro é usado o: */ Integer.parseInt(variavel);
```