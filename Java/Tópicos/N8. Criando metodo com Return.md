  

  

```Java
/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package javaapplication5;
import java.util.Scanner;
import java.util.Random;
/**
 *
 * @author Luciene
 */
public class JavaApplication5 {
private static final double largura = 10.0;
  
    public static void main(String[] args) {
        Scanner ler = new Scanner(System.in);
       
        System.out.println("Coloque o seu comprimento: ");
       double compr = ler.nextDouble();
       double area = calculo( compr, largura );
       System.out.println("a sua area é: "+area);
        
    }

//////////*Explicação abaixo*///////////



/*private static double calculo(double compr, double largura) {
        return largura * compr;
^^^^^^CONSTANTES^^^^^^^^
 private static double calculo(double compr, double largura) {
        return largura * compr; = o retorno será compr vezes largura

Criou uma função que recebe o valor de double compr e de double largura
e essa função retorna o valor de largura multiplicado pelo valor da largura.

que é um metodo que 
CRIADO APOS O MAIN



double area = calculo( compr, largura ); = calculo recebe compr (que o usuario for digitar) e largura(Que é uma constante que nunca vai mudar). e area recebe o valor de calculo

System.out.println("a sua area é: "+area); =  Ela dirá o valor de area 

private static final double largura = 10.0; = o valor de largura será sempre 10 e nao mudara, para usar o 10 em alguma conta a largura sera uma opção, pois ela nunca mudará, pois ela é uma constante ou seja, ela não mudará
ANTES DO MAIN 

  System.out.println("a sua area é: "+area); = Vai ser colocado na tela o valor de area e o + que vai concatenar a variavel area, que vai possibilitar o valor de area
^^^^^^CONSTANTES^^^^^^^^

    }

    
}

*/
```